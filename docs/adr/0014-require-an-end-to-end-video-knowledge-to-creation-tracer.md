# Require a real video knowledge-to-creation tracer

The first milestone is accepted only when one unchanged raw product video is analyzed into a validated persistent profile, published and retrieved through the Agent interface, selected by the video consumer, extracted by opaque asset ID into task-scoped frame and clip artifacts, shown in storyboard confirmation, and used as a real assembly input. Schema, cache, or script-only delivery is explicitly insufficient.
