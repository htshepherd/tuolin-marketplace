# Add a dedicated LinkedIn search knowledge context

Add a `linkedin_search` downstream Agent-interface context for official product identity, approved external naming, application scenarios, and sales-use terms consumed by `tuolin-linkedin-search`. Do not reuse `linkedin_post`, campaign artifacts, or raw knowledge files, because prospect keyword generation and invitation drafting need a bounded product-grounded input without inheriting publishing plans or bypassing the verified knowledge interface.
