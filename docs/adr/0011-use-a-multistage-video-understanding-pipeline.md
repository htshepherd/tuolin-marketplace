# Build video profiles with a multistage understanding pipeline

Video profiles are produced by deterministic media inspection and extraction, frame-quality and duplicate checks, optional time-coded speech transcription, multimodal semantic interpretation, and final structural validation. Media metadata and timestamps come from tools rather than model guesses, while the model interprets ordered visual and audio evidence. This division improves action understanding and keeps failures, uncertainty, and review boundaries explicit.
