# Use the official Codex Chrome extension for LinkedIn search

The first `tuolin-linkedin-search` release operates LinkedIn through Codex's official Chrome extension so it can use the operator's existing signed-in Chrome profile while keeping the task in a dedicated tab group. It does not launch a repository-owned headless browser and does not require full CDP for ordinary search, inspection, and connection actions; the built-in Browser remains out of scope for this release.
