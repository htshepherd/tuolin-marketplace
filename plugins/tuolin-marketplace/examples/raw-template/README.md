# Raw Template

This directory documents the expected `raw/` structure. Real enterprise files must stay local and must not be committed.

Use Codex to initialize a local project when needed. The internal initializer can create the raw template directories in the target project.

